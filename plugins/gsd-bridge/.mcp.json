{
  "mcpServers": {
    "gsd-bridge": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "GSD_BRIDGE_DIR": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
